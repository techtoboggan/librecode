{
  "$schema": "https://v2-8-13.turborepo.dev/schema.json",
  "globalEnv": ["CI", "LIBRECODE_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "LIBRECODE_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "librecode#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@librecode/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
